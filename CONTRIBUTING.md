# Contributing

Use synthetic examples with explicit units. Keep source analysis semantics and
measurement scope intact. Add regression coverage for ID mapping, provenance or
format changes, and run `python -m unittest discover -s tests -v`.

Do not commit local experimental data, private presets, instrument credentials
or vendor SDKs. Changes are contributed under the MIT license.
